import React from 'react';
import PlaceholderPage from '../PlaceholderPage';

const PayrollRunsPage: React.FC = () => {
    return <PlaceholderPage title="Payroll Runs" />;
};

export default PayrollRunsPage;
