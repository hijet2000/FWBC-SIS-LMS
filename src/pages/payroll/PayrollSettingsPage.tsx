import React from 'react';
import PlaceholderPage from '../PlaceholderPage';

const PayrollSettingsPage: React.FC = () => {
    return <PlaceholderPage title="Payroll Settings" />;
};

export default PayrollSettingsPage;
