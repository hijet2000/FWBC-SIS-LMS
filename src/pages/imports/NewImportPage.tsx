import React from 'react';
import PlaceholderPage from '../PlaceholderPage';

const NewImportPage: React.FC = () => {
    return <PlaceholderPage title="New Data Import" />;
};

export default NewImportPage;
