import React from 'react';
import PlaceholderPage from '../PlaceholderPage';

const BackupSettingsPage: React.FC = () => {
    return <PlaceholderPage title="Backup Settings" />;
};

export default BackupSettingsPage;
