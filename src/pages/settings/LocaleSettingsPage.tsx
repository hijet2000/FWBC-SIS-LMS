import React from 'react';
import PlaceholderPage from '../PlaceholderPage';

const LocaleSettingsPage: React.FC = () => {
    return <PlaceholderPage title="Locale Settings" />;
};

export default LocaleSettingsPage;
